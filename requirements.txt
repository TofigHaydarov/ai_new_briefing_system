# Runtime dependencies (Docker image installs ONLY this file)
 
# --- provided ai/ package ---
numpy==2.3.2
pydantic==2.13.5
 
# LLM / embedding providers: install the ones you use.
# Default per TOPIC.md: Anthropic (LLM) + OpenAI (embeddings)
anthropic==0.64.0
openai==1.99.9
# Gemini is optional (adds image size). Enable only if EMBEDDING_PROVIDER=gemini:
# google-genai==X.Y.Z   (check the latest with: pip index versions google-genai)
 
# --- SE layer ---
aiohttp==3.14.3
feedparser==6.0.12
beautifulsoup4==4.13.4
tenacity==9.1.2
pydantic-settings==2.10.1
APScheduler==3.11.0
typer>=0.27.0